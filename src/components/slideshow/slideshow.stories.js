import '../../styles/theme.css';
import './slideshow.js';

export default {
  title: 'Components/Slideshow'
};

const Template = () => '<bf-slideshow></bf-slideshow>';

export const Primary = Template.bind({});